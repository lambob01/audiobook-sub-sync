# Required
SESSION_SECRET=your-secret-at-least-32-chars-here

# Your server's LAN IP or hostname (how clients will access it)
ORIGIN=http://192.168.1.100:3000

# Set to true if using HTTPS reverse proxy (Caddy, NGINX), false for plain HTTP
HTTPS=false
